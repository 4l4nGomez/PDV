<Window x:Class="BakeryPOS.Views.CheckoutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BakeryPOS"
        Title="Procesar Pago" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" Background="#F3F4F6"
        WindowStyle="None" AllowsTransparency="True">
    
    <Window.Resources>
        <local:ChangeLabelConverter x:Key="LabelConverter"/>
        <local:DifferenceToBrushConverter x:Key="ColorConverter"/>
    </Window.Resources>

    <Border Background="White" Margin="15" CornerRadius="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#CCCCCC"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Finalizar Venta" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#1F2937"/>

            <!-- Resumen Total -->
            <Border Grid.Row="1" Background="#F9FAFB" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                <Grid>
                    <TextBlock Text="TOTAL A COBRAR" FontSize="12" FontWeight="Bold" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TicketTotal, StringFormat=C}" FontSize="32" FontWeight="Black" Foreground="#1F2937" HorizontalAlignment="Right"/>
                </Grid>
            </Border>

            <!-- Entrada de Efectivo -->
            <StackPanel Grid.Row="2">
                <TextBlock Text="MONTO RECIBIDO ($)" FontSize="14" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,8"/>
                <TextBox x:Name="CashInput" Text="{Binding CashReceivedText, UpdateSourceTrigger=PropertyChanged}" 
                         FontSize="42" FontWeight="Black" Height="80" TextAlignment="Center" VerticalContentAlignment="Center"
                         BorderBrush="#3B82F6" BorderThickness="2" Background="White" Padding="5">
                </TextBox>
            </StackPanel>

            <!-- Cambio con Viewbox para asegurar que se vea completo -->
            <Border Grid.Row="3" Margin="0,20,0,20" CornerRadius="10" Padding="15">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChangeDue, Converter={StaticResource ColorConverter}}" Value="Green">
                                <Setter Property="Background" Value="#ECFDF5"/>
                                <Setter Property="BorderBrush" Value="#A7F3D0"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ChangeDue, Converter={StaticResource ColorConverter}}" Value="Red">
                                <Setter Property="Background" Value="#FEF2F2"/>
                                <Setter Property="BorderBrush" Value="#FECACA"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="{Binding ChangeDue, Converter={StaticResource LabelConverter}}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#6B7280"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChangeDue, Converter={StaticResource ColorConverter}}" Value="Green">
                                        <Setter Property="Foreground" Value="#059669"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ChangeDue, Converter={StaticResource ColorConverter}}" Value="Red">
                                        <Setter Property="Foreground" Value="#991B1B"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    
                    <Viewbox Height="60" Margin="0,5,0,0">
                        <TextBlock Text="{Binding ChangeDue, StringFormat=C}" FontWeight="Black" HorizontalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#1F2937"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChangeDue, Converter={StaticResource ColorConverter}}" Value="Green">
                                            <Setter Property="Foreground" Value="#10B981"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ChangeDue, Converter={StaticResource ColorConverter}}" Value="Red">
                                            <Setter Property="Foreground" Value="#DC2626"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Viewbox>
                </StackPanel>
            </Border>

            <!-- Acciones -->
            <UniformGrid Grid.Row="4" Columns="2" Height="50" Margin="0,0,0,0">
                <Button Content="CANCELAR (Esc)" Click="Cancel_Click" IsCancel="True" Background="#E5E7EB" Foreground="#374151" FontWeight="Bold" BorderThickness="0" Margin="0,0,5,0" Cursor="Hand"/>
                <Button Content="COBRAR (Enter)" Click="Confirm_Click" IsDefault="True" Background="#10B981" Foreground="White" FontWeight="Bold" BorderThickness="0" Margin="5,0,0,0" Cursor="Hand"/>
            </UniformGrid>
        </Grid>
    </Border>
</Window>
