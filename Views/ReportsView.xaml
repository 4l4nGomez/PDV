<UserControl x:Class="BakeryPOS.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Reportes de Ventas y Movimientos (Admin)" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding StartDate}" Width="150" Margin="0,0,20,0"/>
            
            <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding EndDate}" Width="150" Margin="0,0,20,0"/>

            <Button Content="Filtrar Reporte" Command="{Binding LoadDataCommand}" Padding="15,5" Background="#6366F1" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
            
            <TextBlock Text="Ventas Totales: " VerticalAlignment="Center" Margin="40,0,5,0" FontSize="18"/>
            <TextBlock Text="{Binding TotalSales, StringFormat=C}" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="#10B981"/>
        </StackPanel>

        <TabControl Grid.Row="2">
            <TabItem Header="Ventas Detalladas">
                <DataGrid ItemsSource="{Binding Sales}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Fecha/Hora" Binding="{Binding SaleDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                        <DataGridTextColumn Header="Cajero" Binding="{Binding User.Username}" Width="100"/>
                        <DataGridTextColumn Header="Método" Binding="{Binding PaymentMethod}" Width="100"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" Width="100" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="Cortes y Turnos">
                <DataGrid ItemsSource="{Binding Shifts}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding User.Username}" Width="100"/>
                        <DataGridTextColumn Header="Inicio" Binding="{Binding StartTime, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                        <DataGridTextColumn Header="Fin" Binding="{Binding EndTime, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                        <DataGridTextColumn Header="Efectivo Inicial" Binding="{Binding StartingCash, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="Efectivo Final" Binding="{Binding ActualEndingCash, StringFormat=C}" Width="100" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Movimientos de Caja">
                <DataGrid ItemsSource="{Binding Movements}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding MovementDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding Amount, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding User.Username}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
