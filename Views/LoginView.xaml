<UserControl x:Class="BakeryPOS.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordInput}"/>
    </UserControl.InputBindings>
    <Grid Background="#F3F4F6">
        <Border Background="White" Width="350" Height="400" CornerRadius="10" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Drop shadow effect -->
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="20" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="PUNTO DE VENTA" FontSize="24" FontWeight="Bold" Foreground="#1E3A8A" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Inicia sesión para continuar" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,0,0,10" FontWeight="Bold"/>

                <TextBlock Text="Usuario" FontWeight="Bold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Users}" 
                          SelectedItem="{Binding SelectedUser}" 
                          DisplayMemberPath="Username"
                          Height="40" Padding="10,0" FontSize="16" Margin="0,0,0,20"
                          VerticalContentAlignment="Center">
                    <ComboBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>
                
                <TextBlock Text="Contraseña" FontWeight="Bold" Margin="0,0,0,5"/>
                <!-- Usando PasswordBox para que los caracteres no se vean (se verán como puntos) -->
                <PasswordBox x:Name="PasswordInput" Height="40" Padding="5" FontSize="16" Margin="0,0,0,30">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>
                
                <Button Content="INGRESAR" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordInput}" Height="45" Background="#2563EB" Foreground="White" FontSize="16" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
