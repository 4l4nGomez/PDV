<UserControl x:Class="BakeryPOS.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding AddProductCommand}"/>
    </UserControl.InputBindings>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Catálogo e Inventario" FontSize="24" FontWeight="Bold" Foreground="#1F2937"/>
            <Button Grid.Column="1" Command="{Binding ExportToExcelCommand}" Background="#10B981" Foreground="White" BorderThickness="0" Padding="15,8" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Exportar Inventario a Excel" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20" Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <!-- Código -->
                    <ColumnDefinition Width="*"/>    <!-- Nombre (Flexible) -->
                    <ColumnDefinition Width="Auto"/> <!-- Precio -->
                    <ColumnDefinition Width="Auto"/> <!-- Botón -->
                </Grid.ColumnDefinitions>
                
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="CÓDIGO" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,5"/>
                    <TextBox x:Name="CodeInput" Text="{Binding NewProductCode, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="35" VerticalContentAlignment="Center" Padding="8,0" TabIndex="1"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="NOMBRE DEL PRODUCTO" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewProductName, UpdateSourceTrigger=PropertyChanged}" Height="35" VerticalContentAlignment="Center" Padding="8,0" TabIndex="2"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Margin="0,0,15,0">
                    <TextBlock Text="PRECIO" FontSize="10" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewProductPrice, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="35" VerticalContentAlignment="Center" Padding="8,0" TabIndex="3"/>
                </StackPanel>

                <Button Grid.Column="3" Content="+ AGREGAR PRODUCTO" Command="{Binding AddProductCommand}" IsDefault="True" VerticalAlignment="Bottom" Height="35" Padding="20,0" Background="#10B981" Foreground="White" BorderThickness="0" FontWeight="Bold" TabIndex="4">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Products}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" BeginningEdit="ProductsGrid_BeginningEdit">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Code}" Width="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Stock (Mostrador)" Binding="{Binding Stock}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Días Disponible" Binding="{Binding AvailableDays}" Width="120"/>
                <DataGridTemplateColumn Header="Acciones" Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Modificar" Command="{Binding DataContext.UpdateProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#3B82F6" Foreground="White" Padding="5,2" BorderThickness="0" Margin="0,0,5,0"/>
                                <Button Content="Eliminar" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#EF4444" Foreground="White" Padding="5,2" BorderThickness="0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
